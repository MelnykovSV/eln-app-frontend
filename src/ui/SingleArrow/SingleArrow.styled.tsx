import styled from "@emotion/styled";

const Container = styled.div`
  width: 200px;
  svg {
    aspect-ratio: 12 / 1;
    width: 185px;
    display: block;
    margin: 0 auto;
  }
`;

export default Container;
